SELECT id_pergunta, resposta
FROM respostas
WHERE id_utilizador = '{{ appsmith.user.email }}'
  AND dominio = 'económico'
  AND ano = (
    SELECT MAX(ano)
    FROM respostas
    WHERE id_utilizador = '{{ appsmith.user.email }}'
      AND dominio = 'económico'
  );